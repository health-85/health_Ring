package com.healthy.rvigor.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.healthy.rvigor.dao.entity.EmotionDBEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "EMOTION_DBENTITY".
*/
public class EmotionDBEntityDao extends AbstractDao<EmotionDBEntity, Long> {

    public static final String TABLENAME = "EMOTION_DBENTITY";

    /**
     * Properties of entity EmotionDBEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Uid = new Property(1, long.class, "uid", false, "UID");
        public final static Property IsupLoadToServer = new Property(2, int.class, "isupLoadToServer", false, "ISUP_LOAD_TO_SERVER");
        public final static Property TempDay = new Property(3, long.class, "tempDay", false, "TEMP_DAY");
        public final static Property DeviceName = new Property(4, String.class, "deviceName", false, "DEVICE_NAME");
        public final static Property DeviceMacAddress = new Property(5, String.class, "deviceMacAddress", false, "DEVICE_MAC_ADDRESS");
        public final static Property TempJsonData = new Property(6, String.class, "tempJsonData", false, "TEMP_JSON_DATA");
    }


    public EmotionDBEntityDao(DaoConfig config) {
        super(config);
    }
    
    public EmotionDBEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"EMOTION_DBENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"UID\" INTEGER NOT NULL ," + // 1: uid
                "\"ISUP_LOAD_TO_SERVER\" INTEGER NOT NULL ," + // 2: isupLoadToServer
                "\"TEMP_DAY\" INTEGER NOT NULL ," + // 3: tempDay
                "\"DEVICE_NAME\" TEXT," + // 4: deviceName
                "\"DEVICE_MAC_ADDRESS\" TEXT," + // 5: deviceMacAddress
                "\"TEMP_JSON_DATA\" TEXT);"); // 6: tempJsonData
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"EMOTION_DBENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, EmotionDBEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUid());
        stmt.bindLong(3, entity.getIsupLoadToServer());
        stmt.bindLong(4, entity.getTempDay());
 
        String deviceName = entity.getDeviceName();
        if (deviceName != null) {
            stmt.bindString(5, deviceName);
        }
 
        String deviceMacAddress = entity.getDeviceMacAddress();
        if (deviceMacAddress != null) {
            stmt.bindString(6, deviceMacAddress);
        }
 
        String tempJsonData = entity.getTempJsonData();
        if (tempJsonData != null) {
            stmt.bindString(7, tempJsonData);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, EmotionDBEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUid());
        stmt.bindLong(3, entity.getIsupLoadToServer());
        stmt.bindLong(4, entity.getTempDay());
 
        String deviceName = entity.getDeviceName();
        if (deviceName != null) {
            stmt.bindString(5, deviceName);
        }
 
        String deviceMacAddress = entity.getDeviceMacAddress();
        if (deviceMacAddress != null) {
            stmt.bindString(6, deviceMacAddress);
        }
 
        String tempJsonData = entity.getTempJsonData();
        if (tempJsonData != null) {
            stmt.bindString(7, tempJsonData);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public EmotionDBEntity readEntity(Cursor cursor, int offset) {
        EmotionDBEntity entity = new EmotionDBEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // uid
            cursor.getInt(offset + 2), // isupLoadToServer
            cursor.getLong(offset + 3), // tempDay
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // deviceName
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // deviceMacAddress
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6) // tempJsonData
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, EmotionDBEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUid(cursor.getLong(offset + 1));
        entity.setIsupLoadToServer(cursor.getInt(offset + 2));
        entity.setTempDay(cursor.getLong(offset + 3));
        entity.setDeviceName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setDeviceMacAddress(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setTempJsonData(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(EmotionDBEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(EmotionDBEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(EmotionDBEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
