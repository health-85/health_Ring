package com.healthy.rvigor.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.healthy.rvigor.dao.entity.SleepDBEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SLEEP_DBENTITY".
*/
public class SleepDBEntityDao extends AbstractDao<SleepDBEntity, Long> {

    public static final String TABLENAME = "SLEEP_DBENTITY";

    /**
     * Properties of entity SleepDBEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Uid = new Property(1, long.class, "uid", false, "UID");
        public final static Property IsupLoadToServer = new Property(2, int.class, "isupLoadToServer", false, "ISUP_LOAD_TO_SERVER");
        public final static Property SleepDay = new Property(3, long.class, "sleepDay", false, "SLEEP_DAY");
        public final static Property DeviceName = new Property(4, String.class, "deviceName", false, "DEVICE_NAME");
        public final static Property DeviceMacAddress = new Property(5, String.class, "deviceMacAddress", false, "DEVICE_MAC_ADDRESS");
        public final static Property StartDateTime = new Property(6, long.class, "startDateTime", false, "START_DATE_TIME");
        public final static Property Sleeplength = new Property(7, long.class, "sleeplength", false, "SLEEPLENGTH");
        public final static Property SleepJsonData = new Property(8, String.class, "sleepJsonData", false, "SLEEP_JSON_DATA");
    }


    public SleepDBEntityDao(DaoConfig config) {
        super(config);
    }
    
    public SleepDBEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SLEEP_DBENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"UID\" INTEGER NOT NULL ," + // 1: uid
                "\"ISUP_LOAD_TO_SERVER\" INTEGER NOT NULL ," + // 2: isupLoadToServer
                "\"SLEEP_DAY\" INTEGER NOT NULL ," + // 3: sleepDay
                "\"DEVICE_NAME\" TEXT," + // 4: deviceName
                "\"DEVICE_MAC_ADDRESS\" TEXT," + // 5: deviceMacAddress
                "\"START_DATE_TIME\" INTEGER NOT NULL ," + // 6: startDateTime
                "\"SLEEPLENGTH\" INTEGER NOT NULL ," + // 7: sleeplength
                "\"SLEEP_JSON_DATA\" TEXT);"); // 8: sleepJsonData
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SLEEP_DBENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SleepDBEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUid());
        stmt.bindLong(3, entity.getIsupLoadToServer());
        stmt.bindLong(4, entity.getSleepDay());
 
        String deviceName = entity.getDeviceName();
        if (deviceName != null) {
            stmt.bindString(5, deviceName);
        }
 
        String deviceMacAddress = entity.getDeviceMacAddress();
        if (deviceMacAddress != null) {
            stmt.bindString(6, deviceMacAddress);
        }
        stmt.bindLong(7, entity.getStartDateTime());
        stmt.bindLong(8, entity.getSleeplength());
 
        String sleepJsonData = entity.getSleepJsonData();
        if (sleepJsonData != null) {
            stmt.bindString(9, sleepJsonData);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SleepDBEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUid());
        stmt.bindLong(3, entity.getIsupLoadToServer());
        stmt.bindLong(4, entity.getSleepDay());
 
        String deviceName = entity.getDeviceName();
        if (deviceName != null) {
            stmt.bindString(5, deviceName);
        }
 
        String deviceMacAddress = entity.getDeviceMacAddress();
        if (deviceMacAddress != null) {
            stmt.bindString(6, deviceMacAddress);
        }
        stmt.bindLong(7, entity.getStartDateTime());
        stmt.bindLong(8, entity.getSleeplength());
 
        String sleepJsonData = entity.getSleepJsonData();
        if (sleepJsonData != null) {
            stmt.bindString(9, sleepJsonData);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public SleepDBEntity readEntity(Cursor cursor, int offset) {
        SleepDBEntity entity = new SleepDBEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // uid
            cursor.getInt(offset + 2), // isupLoadToServer
            cursor.getLong(offset + 3), // sleepDay
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // deviceName
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // deviceMacAddress
            cursor.getLong(offset + 6), // startDateTime
            cursor.getLong(offset + 7), // sleeplength
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8) // sleepJsonData
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SleepDBEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUid(cursor.getLong(offset + 1));
        entity.setIsupLoadToServer(cursor.getInt(offset + 2));
        entity.setSleepDay(cursor.getLong(offset + 3));
        entity.setDeviceName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setDeviceMacAddress(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setStartDateTime(cursor.getLong(offset + 6));
        entity.setSleeplength(cursor.getLong(offset + 7));
        entity.setSleepJsonData(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(SleepDBEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(SleepDBEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(SleepDBEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
