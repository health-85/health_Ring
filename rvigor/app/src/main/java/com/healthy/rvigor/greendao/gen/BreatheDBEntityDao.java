package com.healthy.rvigor.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.healthy.rvigor.dao.entity.BreatheDBEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BREATHE_DBENTITY".
*/
public class BreatheDBEntityDao extends AbstractDao<BreatheDBEntity, Long> {

    public static final String TABLENAME = "BREATHE_DBENTITY";

    /**
     * Properties of entity BreatheDBEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Uid = new Property(1, long.class, "uid", false, "UID");
        public final static Property IsupLoadToServer = new Property(2, int.class, "isupLoadToServer", false, "ISUP_LOAD_TO_SERVER");
        public final static Property Day = new Property(3, long.class, "day", false, "DAY");
        public final static Property DeviceName = new Property(4, String.class, "deviceName", false, "DEVICE_NAME");
        public final static Property DeviceMacAddress = new Property(5, String.class, "deviceMacAddress", false, "DEVICE_MAC_ADDRESS");
        public final static Property BreatheJsonData = new Property(6, String.class, "breatheJsonData", false, "BREATHE_JSON_DATA");
        public final static Property Hypopnea = new Property(7, int.class, "hypopnea", false, "HYPOPNEA");
        public final static Property BlockLen = new Property(8, int.class, "blockLen", false, "BLOCK_LEN");
        public final static Property ChaosIndex = new Property(9, int.class, "chaosIndex", false, "CHAOS_INDEX");
        public final static Property PauseCount = new Property(10, int.class, "pauseCount", false, "PAUSE_COUNT");
    }


    public BreatheDBEntityDao(DaoConfig config) {
        super(config);
    }
    
    public BreatheDBEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"BREATHE_DBENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"UID\" INTEGER NOT NULL ," + // 1: uid
                "\"ISUP_LOAD_TO_SERVER\" INTEGER NOT NULL ," + // 2: isupLoadToServer
                "\"DAY\" INTEGER NOT NULL ," + // 3: day
                "\"DEVICE_NAME\" TEXT," + // 4: deviceName
                "\"DEVICE_MAC_ADDRESS\" TEXT," + // 5: deviceMacAddress
                "\"BREATHE_JSON_DATA\" TEXT," + // 6: breatheJsonData
                "\"HYPOPNEA\" INTEGER NOT NULL ," + // 7: hypopnea
                "\"BLOCK_LEN\" INTEGER NOT NULL ," + // 8: blockLen
                "\"CHAOS_INDEX\" INTEGER NOT NULL ," + // 9: chaosIndex
                "\"PAUSE_COUNT\" INTEGER NOT NULL );"); // 10: pauseCount
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"BREATHE_DBENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, BreatheDBEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUid());
        stmt.bindLong(3, entity.getIsupLoadToServer());
        stmt.bindLong(4, entity.getDay());
 
        String deviceName = entity.getDeviceName();
        if (deviceName != null) {
            stmt.bindString(5, deviceName);
        }
 
        String deviceMacAddress = entity.getDeviceMacAddress();
        if (deviceMacAddress != null) {
            stmt.bindString(6, deviceMacAddress);
        }
 
        String breatheJsonData = entity.getBreatheJsonData();
        if (breatheJsonData != null) {
            stmt.bindString(7, breatheJsonData);
        }
        stmt.bindLong(8, entity.getHypopnea());
        stmt.bindLong(9, entity.getBlockLen());
        stmt.bindLong(10, entity.getChaosIndex());
        stmt.bindLong(11, entity.getPauseCount());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, BreatheDBEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUid());
        stmt.bindLong(3, entity.getIsupLoadToServer());
        stmt.bindLong(4, entity.getDay());
 
        String deviceName = entity.getDeviceName();
        if (deviceName != null) {
            stmt.bindString(5, deviceName);
        }
 
        String deviceMacAddress = entity.getDeviceMacAddress();
        if (deviceMacAddress != null) {
            stmt.bindString(6, deviceMacAddress);
        }
 
        String breatheJsonData = entity.getBreatheJsonData();
        if (breatheJsonData != null) {
            stmt.bindString(7, breatheJsonData);
        }
        stmt.bindLong(8, entity.getHypopnea());
        stmt.bindLong(9, entity.getBlockLen());
        stmt.bindLong(10, entity.getChaosIndex());
        stmt.bindLong(11, entity.getPauseCount());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public BreatheDBEntity readEntity(Cursor cursor, int offset) {
        BreatheDBEntity entity = new BreatheDBEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // uid
            cursor.getInt(offset + 2), // isupLoadToServer
            cursor.getLong(offset + 3), // day
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // deviceName
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // deviceMacAddress
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // breatheJsonData
            cursor.getInt(offset + 7), // hypopnea
            cursor.getInt(offset + 8), // blockLen
            cursor.getInt(offset + 9), // chaosIndex
            cursor.getInt(offset + 10) // pauseCount
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, BreatheDBEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUid(cursor.getLong(offset + 1));
        entity.setIsupLoadToServer(cursor.getInt(offset + 2));
        entity.setDay(cursor.getLong(offset + 3));
        entity.setDeviceName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setDeviceMacAddress(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setBreatheJsonData(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setHypopnea(cursor.getInt(offset + 7));
        entity.setBlockLen(cursor.getInt(offset + 8));
        entity.setChaosIndex(cursor.getInt(offset + 9));
        entity.setPauseCount(cursor.getInt(offset + 10));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(BreatheDBEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(BreatheDBEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(BreatheDBEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
