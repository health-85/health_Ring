package com.healthy.rvigor.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.healthy.rvigor.dao.entity.SpoDBEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SPO_DBENTITY".
*/
public class SpoDBEntityDao extends AbstractDao<SpoDBEntity, Long> {

    public static final String TABLENAME = "SPO_DBENTITY";

    /**
     * Properties of entity SpoDBEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Uid = new Property(1, long.class, "uid", false, "UID");
        public final static Property SpoDay = new Property(2, long.class, "SpoDay", false, "SPO_DAY");
        public final static Property DeviceName = new Property(3, String.class, "deviceName", false, "DEVICE_NAME");
        public final static Property DeviceMacAddress = new Property(4, String.class, "deviceMacAddress", false, "DEVICE_MAC_ADDRESS");
        public final static Property SpoJsonData = new Property(5, String.class, "spoJsonData", false, "SPO_JSON_DATA");
    }


    public SpoDBEntityDao(DaoConfig config) {
        super(config);
    }
    
    public SpoDBEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SPO_DBENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"UID\" INTEGER NOT NULL ," + // 1: uid
                "\"SPO_DAY\" INTEGER NOT NULL ," + // 2: SpoDay
                "\"DEVICE_NAME\" TEXT," + // 3: deviceName
                "\"DEVICE_MAC_ADDRESS\" TEXT," + // 4: deviceMacAddress
                "\"SPO_JSON_DATA\" TEXT);"); // 5: spoJsonData
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SPO_DBENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SpoDBEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUid());
        stmt.bindLong(3, entity.getSpoDay());
 
        String deviceName = entity.getDeviceName();
        if (deviceName != null) {
            stmt.bindString(4, deviceName);
        }
 
        String deviceMacAddress = entity.getDeviceMacAddress();
        if (deviceMacAddress != null) {
            stmt.bindString(5, deviceMacAddress);
        }
 
        String spoJsonData = entity.getSpoJsonData();
        if (spoJsonData != null) {
            stmt.bindString(6, spoJsonData);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SpoDBEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUid());
        stmt.bindLong(3, entity.getSpoDay());
 
        String deviceName = entity.getDeviceName();
        if (deviceName != null) {
            stmt.bindString(4, deviceName);
        }
 
        String deviceMacAddress = entity.getDeviceMacAddress();
        if (deviceMacAddress != null) {
            stmt.bindString(5, deviceMacAddress);
        }
 
        String spoJsonData = entity.getSpoJsonData();
        if (spoJsonData != null) {
            stmt.bindString(6, spoJsonData);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public SpoDBEntity readEntity(Cursor cursor, int offset) {
        SpoDBEntity entity = new SpoDBEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // uid
            cursor.getLong(offset + 2), // SpoDay
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // deviceName
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // deviceMacAddress
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // spoJsonData
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SpoDBEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUid(cursor.getLong(offset + 1));
        entity.setSpoDay(cursor.getLong(offset + 2));
        entity.setDeviceName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setDeviceMacAddress(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setSpoJsonData(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(SpoDBEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(SpoDBEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(SpoDBEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
